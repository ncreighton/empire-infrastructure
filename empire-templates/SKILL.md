# Empire Templates

Infrastructure component for the Empire ecosystem. Bootstraps new Claude Code projects with recommended skills, configs, and directory structure.

## Trigger Phrases

- "How does Empire Templates work?"
- "Create a new project from template"
- "List available templates"
- "Save a template from existing project"

## Key Components

- **ProjectTemplates** — 9 methods: Project template manager with 6 built-in templates (basic, web-project, python-project, wordpress-site, automation-project, empire-module). Handles project scaffolding, skill installation, and MCP config generation.

## Key Functions

- `create_project(template_name, project_name, project_path, variables)` — Create a new project from template with variable substitution
- `create_template_from_project(project_path, template_name, description)` — Create a reusable template from an existing project
- `list_templates()` — List all available templates (builtin + custom)
- `get_template(name)` — Get a template by name
- `save_template(name, template)` — Save a custom template to disk

## CLI Usage

```bash
python templates.py list                         # List available templates
python templates.py create <template> <name>     # Create project from template
python templates.py show <template>              # Show template details
python templates.py save-from <project> <name>   # Create template from existing project
```

## Dependencies

- **empire-skill-library** (skill installation)
- **empire-mcp-server** (MCP config generation)

## Stats

- **Functions**: 10
- **Classes**: 1
- **Endpoints**: 0
- **Files**: 1
- **Category**: infrastructure
- **Tech Stack**: python

---
*Auto-generated by BrainSkillForge on 2026-03-06*

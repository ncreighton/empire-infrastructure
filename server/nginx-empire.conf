# Empire Services Nginx Reverse Proxy
# Copy to /etc/nginx/sites-available/empire and symlink to sites-enabled
# Then run: certbot --nginx -d n8n.YOURDOMAIN.com -d dashboard.YOURDOMAIN.com -d audit.YOURDOMAIN.com

# n8n
server {
    listen 80;
    server_name n8n.YOURDOMAIN.com;

    location / {
        proxy_pass http://localhost:5678;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
        proxy_cache off;
        chunked_transfer_encoding on;
    }
}

# Empire Dashboard
server {
    listen 80;
    server_name dashboard.YOURDOMAIN.com;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Article Audit
server {
    listen 80;
    server_name audit.YOURDOMAIN.com;

    location / {
        proxy_pass http://localhost:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Empire Toolbox
server {
    listen 80;
    server_name toolbox.YOURDOMAIN.com;

    location / {
        proxy_pass http://localhost:8300;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60;
    }
}

# Mission Control — OpenClaw Web UI
server {
    listen 80;
    server_name control.YOURDOMAIN.com;

    location / {
        proxy_pass http://localhost:18793;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# OpenClaw Canvas Dashboard (served from gateway)
server {
    listen 80;
    server_name canvas.YOURDOMAIN.com;

    location / {
        proxy_pass http://localhost:18793;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Mystic Luna — MoneyClaw Spiritual Readings
server {
    listen 80;
    server_name luna.YOURDOMAIN.com;

    client_max_body_size 10M;

    location / {
        proxy_pass http://localhost:8160;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120;
    }
}

# NOTE: OpenClaw Gateway (port 18789) should NOT be exposed via nginx.
# Access it only through Tailscale for security.
# If you need public webhook access, use a specific path:
# server {
#     listen 80;
#     server_name webhook.YOURDOMAIN.com;
#     location /webhook/ {
#         proxy_pass http://localhost:18789/webhook/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
# }

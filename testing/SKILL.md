# Testing

Infrastructure component for the Empire ecosystem. Multi-level test runner for Project Mesh that verifies syncs, configs, and cross-project integrations.

## Trigger Phrases

- "How does Testing work?"
- "Run smoke tests"
- "Validate empire configs"
- "Run integration tests"
- "Full regression test"

## Key Components

- **TestResult** — 6 methods: Test result container tracking passed/failed/warnings counts per test case.

## Test Levels

| Level | Function | Purpose |
|-------|----------|---------|
| Config | `test_config_validation(hub)` | Validate manifests, system meta.json, VERSION files, global rules |
| Smoke | `test_smoke(hub, project_name)` | Quick post-sync verification: CLAUDE.md exists, .project-mesh dir, sync status, system references |
| Integration | `test_integration(hub)` | Dependency graph consistency, circular dependency detection (DFS), cross-project reference validation, deprecated blacklist consistency |
| Regression | `test_regression(hub)` | Full empire regression — runs config + smoke + integration |

## CLI Usage

```bash
python test_runner.py --smoke --project X     # Smoke test after sync
python test_runner.py --config                # Validate all configs
python test_runner.py --integration           # Cross-system tests
python test_runner.py --regression            # Full empire regression
python test_runner.py --all                   # Everything
```

## Dependencies

- **sync** (sync engine status)
- **tests** (complementary test suites)

## Stats

- **Functions**: 15
- **Classes**: 1
- **Endpoints**: 0
- **Files**: 1
- **Category**: infrastructure
- **Tech Stack**: python

---
*Auto-generated by BrainSkillForge on 2026-03-06*
